Read `.ai/skills/01-clean-code.md` completely, then apply every rule to the code I am currently working on.

1. Check each SOLID principle and flag violations.
2. Review method names, variable names, and class names against the naming rules.
3. Identify functions longer than ~20 lines and extract them.
4. Remove comments that explain WHAT the code does — keep only non-obvious WHY comments.
5. Flag any `any` types without justification, magic numbers, and commented-out code.
6. Run through the pre-submit checklist at the end of the skill file item by item.

Report findings grouped by: **must fix** / **should fix** / **consider**.
