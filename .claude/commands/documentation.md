Read `.ai/skills/06-documentation.md` completely, then write or improve the documentation I describe.

For **JSDoc**: add the required header (description + @authors + @version + @since) to every exported class or interface that is missing it. Add concise one-liner JSDoc to public interface methods.

For **inline comments**: remove comments that explain WHAT. Keep or add comments only where the WHY is non-obvious (hidden constraint, subtle invariant, workaround).

For **README sections**: follow the 12-section structure defined in the skill. Use emojis in headings, bold for key technologies, backticks for commands, and the `<img>` convention for images.

For **`.env.example`**: verify every environment variable is present and add a comment for any non-obvious variable.
